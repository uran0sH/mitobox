# Claude Instructions

Follow the project rules in `AGENTS.md`.

When working on MitoBox:

- Keep the MVP Firecracker-based.
- Preserve the Snapshot / Branch / Sandbox terminology.
- Run `cargo check` after code changes.
